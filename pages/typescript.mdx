# TypeScript Support

### `Schema` type for TypeScript

You can import a type `Schema` to help write your configuration object.

```ts
import { type Schema } from "@interweave/interweave"

const config: Schema = { ... }
```
